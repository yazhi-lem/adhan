# Adhan native Tamil SLM — JAX training stack (Phase 0+)
# Install into a separate venv from the PyTorch pipeline:
#   python3 -m venv .venv-jax && source .venv-jax/bin/activate
#   pip install -r requirements-jax.txt
#
# The jax pinned below is the CPU wheel — training works with no CUDA toolchain
# (see docs/CPU_TRAINING.md). For GPU/TPU use the packaged extra instead:
#   pip install -e ".[jax-cuda]"        # or: pip install "jax[cuda12]"

jax>=0.4.30
jaxlib>=0.4.30
flax>=0.8.5
optax>=0.2.2
orbax-checkpoint>=0.5.0
mlflow>=2.14.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.66.0

# Data / corpus (optional, for packed streaming shards)
grain>=0.2.0 ; python_version >= "3.10"
datasets>=2.14.0

# --- open-tamil (base Tamil-NLP layer) ---------------------------------------
# MIT-licensed. Used across tokenizer tests (segmentation cross-check), eval/
# (sandhi/stemmer morphology probes, kid-level prompt lexicon, n-gram
# baseline), and corpus tooling (legacy-encoding normalization, Tanglish
# transliteration). Never imported by the tokenizer's hot encode/decode path —
# see src/adhan_slm/external/open_tamil_bridge.py for the full rationale.
open-tamil>=1.1

# --- Yazh foundation (base requirement) --------------------------------------
# Adhan is the foundational language model of the Yazh ecosystem and builds on the
# shared Yazh base (config, data schemas, serving contracts). Install from git:
#   pip install "yazh-kutty @ git+https://github.com/yazhi-lem/yazh-kutty.git"
# Deployment goes through yazhi-api (private):
#   pip install "yazhi-api @ git+https://github.com/yazhi-lem/yazhi-api.git"
# (Uncomment once the Yazh packages publish a setup.py / pyproject.toml.)
# yazh-kutty @ git+https://github.com/yazhi-lem/yazh-kutty.git
# yazhi-api  @ git+https://github.com/yazhi-lem/yazhi-api.git

# The swaram/aksharam tokenizer cores are pure-python and work without this file
# at all (no hard tokenizer dependency); JAX only accelerates batch encoding
# (see tokenizer/jax_encode.py), and open-tamil above is optional too — tests
# and eval/ that use it skip cleanly if it's absent.
