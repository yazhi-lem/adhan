# Adhan native Tamil SLM — JAX training stack (Phase 0+)
# Install into a separate venv from the PyTorch pipeline:
#   python3 -m venv .venv-jax && source .venv-jax/bin/activate
#   pip install -r requirements-jax.txt
#
# For GPU/TPU, install the matching jax wheel per https://github.com/google/jax
# (e.g. pip install "jax[cuda12]") instead of the CPU jax pinned below.

jax>=0.4.30
jaxlib>=0.4.30
flax>=0.8.5
optax>=0.2.2
orbax-checkpoint>=0.5.0
mlflow>=2.14.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.66.0

# Data / corpus (optional, for packed streaming shards)
grain>=0.2.0 ; python_version >= "3.10"
datasets>=2.14.0

# The swaram tokenizer core is pure-python (no external tokenizer dependency).
