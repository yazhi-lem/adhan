# Phase 2 data-collection pipeline (src/data_scraper/): scraping, HF export.
# For the JAX SLM training stack, see requirements-jax.txt instead.
requests>=2.31.0
beautifulsoup4>=4.12.0
datasets>=2.14.0
numpy>=1.24.0

# Tamil-script utilities (legacy-encoding detection, transliteration, lexicons)
# used by the corpus-building pipeline in src/data_scraper/. MIT-licensed.
open-tamil>=1.1
